[project]
name = "news-summarizer-ui"
version = "0.1.0"
description = "A Streamlit UI for showing news article summaries using FastAPI backend."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "ruff>=0.13.1",
    "streamlit>=1.49.1",
]
